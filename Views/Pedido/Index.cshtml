@model IEnumerable<Projeto_Final.Models.Pedido>

@{
    ViewData["Title"] = "Index";
}

<h1>Meus pedidos</h1>

<table class="table">
    <thead>
        <tr>
            <th>Código do pedido</th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                <td>
                    @Html.DisplayFor(modelItem => item.Id)
                </td>

            </td>
        </tr>
}
    </tbody>
</table>
